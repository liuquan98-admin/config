# Kitty 模块化配置自动加载
# 严格正确加载顺序：字体 → UI窗口 → 快捷键 → 鼠标 → 配色主题 → 环境性能
# 全部文件都在 conf.d 子目录下，必须补全 conf.d/ 路径前缀
include conf.d/00-font.conf
include conf.d/01-ui.conf
include conf.d/02-key.conf
include conf.d/03-mouse.conf
include conf.d/04-color.conf
include conf.d/05-env.conf

# 根目录主题软链接，全局生效，放在所有模块最后兜底加载
include current-theme.conf
